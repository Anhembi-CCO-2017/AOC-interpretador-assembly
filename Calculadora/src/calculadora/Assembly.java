package calculadora;

/*
 * Primeiro identificar se existe caracteres especiais.
 * Splitar linhas
 * Depois procura um 'MOV' NA PRIMEIRA LINHA
 *  Se achar
 *      le o codigo linha por linha
 *      verificar se na variavel nao existe uma operacao
 *      verifica se é uma op valida (MOV, ADD, SUB, MPY, DIV)
 *          Se sim
 *             verifica os operadores ( X,B) ESPACO,VARIAVEL,VARIAVEL
 *             se tudo certo, move para matriz [OP:0, VARIAVEL:1, VARIAVEL:2]
 *             garante que é um assembly retorna true;
 *          Se nao
 *              retorna false;
 */
public class Assembly {

    public Assembly() {
    }

    public boolean isAssembly(valor) {

    }
    
}
